security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User\User
                property: email
    firewalls:
        login:
            pattern: ^/v1/auth/login_check
            user_checker: App\Security\UserChecker
            stateless: true
            login_throttling:
                max_attempts: 10
                interval: '5 minute'
            json_login:
                check_path: /v1/auth/login_check
                success_handler: App\Security\Authentication\TwoFactorAuthenticationSuccessHandler
                failure_handler: lexik_jwt_authentication.handler.authentication_failure

        api:
            provider: app_user_provider
            user_checker: App\Security\UserChecker
            stateless: true
            entry_point: jwt
            jwt: ~
            refresh_jwt:
                check_path: gesdinet_jwt_refresh_token
    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        - { path: ^/v1/auth/login_check,                 roles: PUBLIC_ACCESS }
        - { path: ^/v1/auth/register,                    roles: PUBLIC_ACCESS }
        - { path: ^/v1/auth/verify,                      roles: PUBLIC_ACCESS }
        - { path: ^/v1/auth/request_verification_resend, roles: PUBLIC_ACCESS }
        - { path: ^/v1/auth/reset_password,              roles: PUBLIC_ACCESS }
        - { path: ^/v1/auth/request_password_reset,      roles: PUBLIC_ACCESS }
        - { path: ^/v1/auth/token_refresh,               roles: PUBLIC_ACCESS }
        - { path: ^/v1/webhook,                          roles: PUBLIC_ACCESS }
        - { path: ^/v1/doc,                              roles: PUBLIC_ACCESS }
        - { path: ^/v1,                                  roles: IS_AUTHENTICATED_FULLY }

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
